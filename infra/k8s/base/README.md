**kustomize base manifests**

This directory contains our base k8s manifests used with kustomize. It is intended to modularize each core component we use to build our cluster in a way that is environment-agnostic.
